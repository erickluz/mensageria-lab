spring:
  application:
    name: payment-consumer
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  jackson:
    serialization:
      write-dates-as-timestamps: false

logging:
  level:
    org.springframework.amqp.retry: INFO

server:
  port: 8081
